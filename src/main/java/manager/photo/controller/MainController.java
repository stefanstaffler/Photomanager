package manager.photo.controller;

import manager.photo.view.BaseView;

public class MainController extends BaseController {
    public MainController(BaseView baseView) {
        super(baseView);
    }
}
